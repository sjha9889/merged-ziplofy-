<article class="product-card">
  <div class="thumb">
    <img src="{{ product.image }}" alt="{{ product.title }}">
  </div>
  <h3>{{ product.title }}</h3>
  <p class="price">${{ product.price }}</p>
</article>

