<div class="hero-product-card">
  <div class="hero-product-image">
    <img src="{{ product.image }}" alt="{{ product.title }}">
    {% if product.badge %}
    <div class="hero-product-badge">{{ product.badge }}</div>
    {% endif %}
    <div class="hero-product-overlay">
      <button class="hero-product-quick-view">Quick View</button>
    </div>
  </div>
  <div class="hero-product-content">
    <div class="hero-product-rating">
      {% assign rating = product.rating | default: 5 %}
      {% if rating >= 1 %}<span class="stars">★</span>{% endif %}
      {% if rating >= 2 %}<span class="stars">★</span>{% endif %}
      {% if rating >= 3 %}<span class="stars">★</span>{% endif %}
      {% if rating >= 4 %}<span class="stars">★</span>{% endif %}
      {% if rating >= 5 %}<span class="stars">★</span>{% endif %}
      <span class="rating-count">({{ product.reviewCount | default: 0 }})</span>
    </div>
    <h3 class="hero-product-name">
      <a href="product-detail.html?id={{ product.id }}" class="hero-product-name-link">{{ product.title }}</a>
    </h3>
    {% if product.shortDescription %}
    <p class="hero-product-description">{{ product.shortDescription }}</p>
    {% endif %}
    <div class="hero-product-price">
      <span class="current-price">₹{{ product.price }}</span>
      {% if product.originalPrice %}
      <span class="original-price">₹{{ product.originalPrice }}</span>
      {% endif %}
      {% if product.discount %}
      <span class="discount">{{ product.discount }}% OFF</span>
      {% endif %}
    </div>
    <div class="hero-product-actions">
      <button class="hero-product-btn">Add to Cart</button>
      <button class="hero-product-wishlist">♡</button>
    </div>
  </div>
</div>

