{% layout 'main' %}
<div class="container py-5">
  <h1 class="mb-4">{{ page.title | default: 'Contact' }}</h1>
  <p class="lead">{{ store.description | default: 'We would love to hear from you.' }}</p>
  <p class="text-muted">Use your store admin to add phone, email, and address details to this page when ready.</p>
</div>
